const React = require('react');

const Bill = React.createClass({
  render: function () {
    return (
      <div>
        hello from the bills component
      </div>
    );
  }
});

module.exports = Bill;
